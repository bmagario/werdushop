'use strict';

/**
 * Module dependencies
 */
var path = require('path'),
  config = require(path.resolve('./config/config'));

/**
 * Cost Centers module init function.
 */
module.exports = function (app) {

};
